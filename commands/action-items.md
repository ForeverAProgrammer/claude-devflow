Extract all action items from the text below. Rules:
- List each item as a checkbox: `- [ ] Action item`
- Start each item with a verb (e.g. "Schedule", "Send", "Review", "Fix")
- Include the owner if mentioned (e.g. "- [ ] [Sarah] Send the report by Friday")
- Include deadlines if mentioned
- Do not include discussion points, decisions, or context — only concrete tasks
- If no clear action items exist, say so in one sentence

Text:
$ARGUMENTS
